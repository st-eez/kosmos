\* Admission leaves a window taken over concealed on a workspace shown since the start (fails ConvergesWhenSettled, expected).
CONSTANTS
    Win <- MC_Win
    HomeOf <- MC_HomeOf
    Workspaces = {1, 2, 3}
    FirstShown = 1
    MaxEvents = 3
    Adopt = TRUE
    Grace = TRUE
    Gate = TRUE
    AdmitReveal = FALSE
    Backstop = TRUE
    AllowUnreadable = TRUE
    AllowHung = TRUE
    AllowLayoutLag = TRUE
CHECK_DEADLOCK FALSE
SPECIFICATION Spec
INVARIANTS
    ConvergesWhenSettled
