\* An arm for a build that reads another record version stands, and that build finds no record (fails RecordedBeforeHide or NeverStranded, expected).
CONSTANTS
    Win <- MC_Win
    HomeOf <- MC_HomeOf
    Workspaces = {1, 2, 3}
    FirstShown = 1
    MaxEvents = 3
    Adopt = TRUE
    Grace = TRUE
    Gate = FALSE
    Expiry = TRUE
    KosmosOnly = TRUE
    ReadyGate = TRUE
    AdmitReveal = TRUE
    Backstop = TRUE
    AllowUnreadable = TRUE
    AllowHung = TRUE
    AllowLayoutLag = TRUE
    AllowGuardianFailure = TRUE
    AllowProbe = TRUE
CHECK_DEADLOCK FALSE
SPECIFICATION Spec
INVARIANTS
    RecordedBeforeHide
    NeverStranded
