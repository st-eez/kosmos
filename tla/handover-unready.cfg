\* A Kosmos takes over and hands over without a ready guardian, and a crash or no successor leaves the windows with none (fails NeverStranded, expected).
CONSTANTS
    Win <- MC_Win
    HomeOf <- MC_HomeOf
    Workspaces = {1, 2, 3}
    FirstShown = 1
    MaxEvents = 3
    Adopt = TRUE
    Grace = TRUE
    Gate = TRUE
    Expiry = TRUE
    KosmosOnly = TRUE
    ReadyGate = FALSE
    AdmitReveal = TRUE
    Backstop = TRUE
    AllowUnreadable = TRUE
    AllowHung = TRUE
    AllowLayoutLag = TRUE
    AllowGuardianFailure = TRUE
    AllowProbe = TRUE
CHECK_DEADLOCK FALSE
SPECIFICATION Spec
INVARIANTS
    NeverStranded
