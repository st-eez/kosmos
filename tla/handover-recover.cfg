\* Startup recovery in place of the take-over, as before: a restart shows the hidden windows (fails KeepsHidden, expected).
CONSTANTS
    Win <- MC_Win
    HomeOf <- MC_HomeOf
    Workspaces = {1, 2, 3}
    FirstShown = 1
    MaxEvents = 3
    Adopt = FALSE
    Grace = TRUE
    Gate = TRUE
    Expiry = TRUE
    KosmosOnly = TRUE
    ReadyGate = TRUE
    AdmitReveal = TRUE
    Backstop = TRUE
    AllowUnreadable = TRUE
    AllowHung = TRUE
    AllowLayoutLag = TRUE
    AllowGuardianFailure = TRUE
    AllowProbe = TRUE
CHECK_DEADLOCK FALSE
SPECIFICATION Spec
INVARIANTS
    NeverStranded
    KeepsHidden
