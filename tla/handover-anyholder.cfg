\* The guardian leaves for any holder of the lock, as a probe or a Kosmos not yet named, and none restores the windows (fails NeverStranded, expected).
CONSTANTS
    Win <- MC_Win
    HomeOf <- MC_HomeOf
    Workspaces = {1, 2, 3}
    FirstShown = 1
    MaxEvents = 3
    Adopt = TRUE
    Grace = TRUE
    Gate = TRUE
    Expiry = TRUE
    KosmosOnly = FALSE
    ReadyGate = TRUE
    AdmitReveal = TRUE
    Backstop = TRUE
    AllowUnreadable = TRUE
    AllowHung = TRUE
    AllowLayoutLag = TRUE
    AllowGuardianFailure = TRUE
    AllowProbe = TRUE
CHECK_DEADLOCK FALSE
SPECIFICATION Spec
INVARIANTS
    NeverStranded
